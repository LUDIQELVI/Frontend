import { XofCurrencyPipe } from './xof-currency.pipe';

describe('XofCurrencyPipe', () => {
  it('create an instance', () => {
    const pipe = new XofCurrencyPipe();
    expect(pipe).toBeTruthy();
  });
});
